# В репозиторий (и на GitHub Pages) попадает ТОЛЬКО папка docs.
# Рабочие файлы с данными клиентов (xlsx, docx) публиковать нельзя.
/*
!/docs
!/tests
!/server_next
!/.gitignore
# Одноразовый локальный предпросмотр server_next (генерируется awk-командой из ИНСТРУКЦИИ)
/tests/server_next_preview.html
